# Core RL + simulation stack (versions verified against the working environment)
stable-baselines3==2.8.0a2
gymnasium==1.2.3
highway-env==1.10.2
torch==2.9.1

# Numerics / plotting / IO
numpy==2.4.0
scipy==1.16.3
pandas==2.3.3
matplotlib==3.10.8
pygame==2.6.1

# Optional: experiment logging (only needed if you enable the Neptune logger)
# neptune
